/*
 * Default Nextflow config used by nf-test.
 *
 * This keeps nf-test executions aligned with the local developer smoke-test
 * path and avoids requiring outbound network access, container runtime support,
 * or remote publish destinations for structural tests.
 */

includeConfig '../conf/test/disable-telemetry.config'
